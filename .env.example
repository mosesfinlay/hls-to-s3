# AWS / S3
AWS_REGION=us-west-2
S3_BUCKET=your-bucket-name
S3_PREFIX=hls                  # will upload to s3://your-bucket-name/hls/
PUBLIC_ACL=true                # true -> objects public-read; false -> private (use CloudFront+OAC)

# Sync behavior
SYNC_INTERVAL_SECONDS=1        # how often the sidecar syncs
HLS_DIR=/hls                   # container path (don’t change)
PLAYLIST_MAXAGE=1              # Cache-Control for *.m3u8 (seconds)
SEGMENT_MAXAGE=3               # Cache-Control for *.ts (seconds)

# RTMP/HLS tuning
HLS_FRAGMENT_SECONDS=2         # segment length
HLS_PLAYLIST_SECONDS=30        # target rolling window length
